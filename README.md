# Dialogflow-Node.js-Server
## Dialogflow-Nodejs Client

### Apis:-
  - GET "/" -> Check status of server
   - POST "/query" -> Send a query to agent
   - GET "/getAllIntents" -> Get all the intents of the agent
